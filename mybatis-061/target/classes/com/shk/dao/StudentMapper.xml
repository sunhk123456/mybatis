<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--核心配置文件-->
<mapper namespace="com.shk.dao.StudentMapper">
<!--<resultMap id="StudentTeacher" type="student">-->
<!--    <association property="teacher" column="tid" select="getTeacher" javaType="Teacher">-->

<!--    </association>-->
<!--</resultMap>-->
<!--    <select id="getStudentx" resultMap="StudentTeacher" >-->
<!--        select * from student ;-->
<!--    </select>-->

<!--    <select id="getTeacher" resultType="Teacher">-->
<!--        select * from teacher where id=#{id}-->
<!--    </select>-->

    <select id="getStudent2" resultMap="StudentTeacher2">
        select s.id id,s.name name,t.name tname from student s,teacher t where s.tid=t.id;
    </select>
    <resultMap id="StudentTeacher2" type="Student">
        <result property="id" column="id" />
        <association property="teacher"  javaType="Teacher">
            <result property="name" column="tname" />
        </association>
    </resultMap>
</mapper>